/**
 * if(true){
 *      // Do Something
 * }
 */

let state="FL";
let taxPercent=0;

if(state !== 'FL'){
    // will execute only if the condition evalute to true
    taxPercent = 7;
}

console.log(taxPercent);
