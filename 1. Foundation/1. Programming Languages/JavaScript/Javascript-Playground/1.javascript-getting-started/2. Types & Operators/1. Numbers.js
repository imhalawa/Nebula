let price = 20.30;

// Addition
price = price + 20;

// Difference
price = price - 1;

// Division
price = price / 5;

// Multiplication
price =price * 7;

// Modulus
price = price % 2;

// Increment
price++;
++price;

// use typeof operator to query the type of a variable, or the result of a function
console.log(typeof price);

// Operator Precedence https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence
