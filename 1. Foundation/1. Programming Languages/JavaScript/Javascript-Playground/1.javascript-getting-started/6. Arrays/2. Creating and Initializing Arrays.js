// Defining empty array
let values = [];

// Define array with values
let arbitrary_item = [
    {
        name: 'orange',
        price: 15
    },
    "Mohamed Halawa", 2, 30]

// Create Array Object using Array.of(params)
let shopping_item = Array.of(1, 2, 3, 4);

console.log(arbitrary_item);